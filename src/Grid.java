import java.awt.Graphics;

public class Grid {
    int horizontalSize;
    int verticalSize;

    Grid() 
    {
        Cell cell = new Cell(0,0,50);
    }

    void paint(Graphics g)
    {
        
    }
}

//g.setColor(Color.WHITE);
// g.fillRect(i, j, 35, 35);
// g.setColor(Color.BLACK);
// g.drawRect(i, j, Cell.SIZE, 35);